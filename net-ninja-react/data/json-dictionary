"blogs" is considered a "resource", because it's the top-level property of the json 
the json server creates "endpoints", that enable us to interact with the resources like:
    delete, add, edit and get items, for examples